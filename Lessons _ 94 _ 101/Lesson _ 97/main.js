/*
    DOM [Deal With Elements]
    before [Elements || String]
    after [Elements || String]
    append [Elements || String]
    prepend [Elements || String]
    remove
*/

let elemnet = document.getElementById("my-div");
let createdP = document.createElement("p");

// elemnet.remove(); 
